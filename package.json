{
  "name": "x12-835-helper",
  "version": "0.1.0",
  "private": true,
  "description": "A browser-only page that turns an X12 835 remittance advice into a readable document.",
  "repository": {
    "type": "git",
    "url": "https://github.com/zarkara/X12-835-Helper.git"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "Apache-2.0",
  "scripts": {
    "test": "node --test tests/*.test.js",
    "sync-core": "node tools/sync-core.js"
  }
}
